skill: migration-review
plants:
  # non-concurrent index build on a large, write-hot PostgreSQL table
  - file: db/migrate/20260704120000_add_index_to_events.rb
    keywords: [concurrently, concurrent, lock, locks, locking, blocking, blocks]
    min-severity: high
max-findings: 4
